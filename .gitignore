# Godot 4 editor/import cache
.godot/

# Godot mono/C# build artifacts (project is GDScript-only, but the mono editor may emit these)
.mono/
mono_crash.*.json
*.csproj
*.sln

# Exports
export.cfg
export_presets.cfg
*.pck
*.apk
*.aab
*.exe
*.dmg
*.zip

# Depth-tool run artifacts. The COMMITTED outputs are docs/depth-report.md
# and scripts/v3/discovered3.gd; the per-shard JSON and logs are scratch.
tools/out/

# OS cruft
.DS_Store
Thumbs.db
desktop.ini
