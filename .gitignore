# Godot 4 editor/import cache
.godot/

# Godot mono/C# build artifacts (project is GDScript-only, but the mono editor may emit these)
.mono/
mono_crash.*.json
*.csproj
*.sln

# Exports
export.cfg
export_presets.cfg
*.pck
*.apk
*.aab
*.exe
*.dmg
*.zip

# Depth-tool run artifacts. The COMMITTED output is docs/depth-report5.md;
# the per-level JSON, run caches and logs under tools/out/ are scratch.
tools/out/

# Third-party art packs (free, but not ours to redistribute in a public repo).
# The demo glue in scripts/pixel_demo.gd + scenes/pixel_demo.tscn IS tracked; the raw
# sprites live here locally only. Drop the packs back in to run the vibe demo.
assets/pixel/

# OS cruft
.DS_Store
Thumbs.db
desktop.ini
assets/ui/
