# v5 depth report — GENERATED by tools/v5/run_depth5.gd

Do not hand-edit: re-run the depth tool instead. Generated 2026-08-14T14:59:59  **--quick mode: tiny budgets + 2 seeds, indicative only**.

## How to read this

- ONE search pass per level yields TWO readings (docs/v5-transition-spec.md §2.2): Reading A (soft-border) = the random-plan win basin + `skill_gap` + the ladder; Reading B (puzzle) = solvability + `K` + `e1`. A **balance-keyed classifier** picks the headline.
- **The random baseline uses a FREER sampler** (drifted self-avoiding walks between random dock stops), NOT the smart shortest-path decoder. The smart decoder auto-covers intermediate rooms and auto-threads a level's cooperative split, so a "random" gene would do the puzzle's reasoning for free and inflate a constraint level's random-win. The freer sampler produces genuinely varied legal plans that often miss rooms and mis-thread the shaft, so its win rate reflects the LEVEL's difficulty, not the decoder's cleverness. (The EA / greedy keep the smart decoder.)
- **Two probe numbers.** *legality rate* = fraction of raw random draws that are even legal (all cards decode under the caps, serve >= 2 rooms); it reflects caps AND the wandering sampler's dead-end rate, so read it as a rough constraint indicator, not a pure cap measure. *legal-plan win rate* = wins among legal draws — **this is the difficulty signal that drives the classifier.**
- **Classifier (balance-keyed):** legal-plan win rate >= 40% (a broad basin of working plans) -> **soft-border**; legal-plan win rate < 40% (few legal plans work) AND the solver is solvable >=7/8 seeds -> **puzzle**; < 40% AND not solvable -> **BROKEN**. The 40% bar is the spec's own forgiving/tutorial bar (§4.1). The `spec-primary` column shows the spec §2.3 raw `w_rand < 5` rule for audit only; it is NOT the driver.
- **Solvability is per-seed** (spec §2.2a): for >= 7/8 TEST seeds, does the solver's set of discovered winning route-sets contain one that wins that seed? (Not "does one genome win every seed" — the right question when a legal win is seed-fragile.)
- **`K` is a secondary quality signal, not the discriminator.** `K (arch)` is the archive estimate (distinct winning `win_signature`s the EA evaluated). `K (exh)` is EXHAUSTIVE ground truth where the capped stop-sequence space is small enough to enumerate (distinct winning per-card served-room structures); `--` means the space was too large and only the archive estimate is available.
- One budget unit = one sim run of THE REAL v5 LEVEL (shipped quota / max_lost / win-lose live, 300 game-second timeout). `WIN -> 1000 + (300 - t_win)`; else `served - 3.0*lost - 0.01*avg_wait`. Optimizers search TRAIN seeds at STEP 0.25; every reported number is a median over the disjoint TEST seeds at STEP 0.10. Solvability is always **relative to this solver at this budget** (Ropossum's caveat): "winnable by our EA within 800 evals on these seeds", never "winnable".

## Classification table

Class is driven by the **legal-plan win rate** (freer sampler) + solvability. `spec-primary` is the raw spec §2.3 `w_rand` rule shown for audit only.

| level | class | legality | legal-plan win rate | solvable | K (arch) | K (exh) | e1 evals | spec-primary |
|---|---|---|---|---|---|---|---|---|
| **R-1** First Run | **soft-border** | 73.8% | 100.00% (200/200) | 2/2 | 1 | 1 | 2 | soft-border |
| **R-2** Two Towers | **soft-border** | 0.7% | 100.00% (116/116) | 2/2 | 3 | 3 | 2 | soft-border |
| **R-3** Handoff | **soft-border** | 0.8% | 44.44% (60/135) | 2/2 | 1 | 3 | 4 | soft-border |
| **R-4** Bottleneck | **soft-border** | 7.8% | 88.00% (176/200) | 2/2 | 60 | 60 | 2 | soft-border |
| **R-5** Relay | **soft-border** | 1.4% | 71.00% (142/200) | 2/2 | 11 | 18 | 2 | soft-border |
| **R-6** Squeeze | **puzzle** | 2.5% | 10.00% (20/200) | 2/2 | 18 | 11 | 26 | soft-border ⚠ |
| **R-7** Express Line | **soft-border** | 7.4% | 77.00% (154/200) | 2/2 | 129 | 170 | 2 | soft-border |
| **R-8** Freight | **soft-border** | 9.8% | 53.50% (107/200) | 2/2 | 101 | — | 2 | soft-border |
| **R-9** Cross-Dock | **puzzle** | 4.3% | 19.50% (39/200) | 2/2 | 121 | — | 14 | soft-border ⚠ |
| **R-10** Rounds | **soft-border** | 16.3% | 96.00% (192/200) | 2/2 | 47 | 41 | 2 | soft-border |

## Validation of the classifier against hand-understood levels

- **R-1 "One Lift" expected SOFT-BORDER** — got **soft-border**, driven by: legal-plan win rate 100.00% >= 40% (broad basin). CORRECT via the balance signal (not a brittle threshold): a broad basin of legal random plans win.
- **R-6 "Squeeze" expected PUZZLE** — got **puzzle**, driven by: legal-plan win rate 10.00% < 40% (few work) AND solvable 2/2. CORRECT via the balance signal. The FREER sampler drops R-6's legal-plan win rate to 10.00% (the smart-decoder version had inflated it to ~6%), toward the ~0 the design intended — genuinely-random legal plans almost never solve it, but the solver is solvable 2/2 seeds via the cooperative split, and the intended split IS among the winning niches. Exhaustive K = 11 confirms the winning structure is small.

Both hand-understood levels are now called correctly by the win/loss BALANCE of legal random plans — a signal with wide margin (R-6 ~10% vs the tutorials' 77-100%), not a knife-edge count threshold.

## R-1 — First Run   [soft-border]

Thesis: *draw one route past the rooms - and one cell between two rooms serves both*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 100.00%, 200/200 legal draws; legality 73.8%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 2/2 seeds.

3 rooms, 1 cards, quota 10 / max_lost 8. Search cost: 232 runs, 2 s wall.

**Class driven by:** legal-plan win rate 100.00% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 73.8% (200 legal of 271) |
| **legal-plan win rate** (freer sampler) | **100.00%** (200/200 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 1 |
| `K` EXHAUSTIVE (ground truth) | 1  — 2 genomes enumerated (stops<=3), 1 distinct served-room structures, 1 winning (>=1/2 seeds) |
| `e1` (evals to first win) | 2 |
| `skill_gap` (ea@B - random best) | +1.2 |
| ladder steps (budget doublings that beat noise 0.00) | 0 |
| best-EA vs thesis structural distance | 0.00 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | -4.2 |

Thesis route-set on TEST seeds: score 1261.60, wins 2/2, served ~11, lost ~0.0.

Ladder (median TEST score vs budget): 50:1262  100:1262  200:1262  400:1262 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  5 cells, serves ["A", "B", "C"]
```

**Verdict.** SOFT-BORDER: a broad basin — 100.00% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap +1.2 over 0 ladder step(s). The EA matches the hand thesis (+0.0). Exhaustive K = 1.

## R-2 — Two Towers   [soft-border]

Thesis: *a wall splits the building - one lift can't cover both towers, so you need two*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 100.00%, 116/116 legal draws; legality 0.7%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 2/2 seeds.

4 rooms, 2 cards, quota 12 / max_lost 10. Search cost: 180 runs, 53 s wall.

**Class driven by:** legal-plan win rate 100.00% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 0.7% (116 legal of 16000) |
| **legal-plan win rate** (freer sampler) | **100.00%** (116/116 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 3 |
| `K` EXHAUSTIVE (ground truth) | 3  — 1296 genomes enumerated (stops<=3), 3 distinct served-room structures, 3 winning (>=1/2 seeds) |
| `e1` (evals to first win) | 2 |
| `skill_gap` (ea@B - random best) | +0.0 |
| ladder steps (budget doublings that beat noise 0.00) | 0 |
| best-EA vs thesis structural distance | 0.00 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +3.5 |

Thesis route-set on TEST seeds: score 1252.90, wins 2/2, served ~13, lost ~0.0.

Ladder (median TEST score vs budget): 50:1253  100:1253  200:1253  400:1253 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  6 cells, serves ["A", "B"]
card 1 line  6 cells, serves ["C", "D"]
```

**Verdict.** SOFT-BORDER: a broad basin — 100.00% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap +0.0 over 0 ladder step(s). The EA matches the hand thesis (+0.0). Exhaustive K = 3.

## R-3 — Handoff   [soft-border]

Thesis: *two wings joined only by the lobby - cross the building by changing lifts there*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 44.44%, 60/135 legal draws; legality 0.8%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 2/2 seeds.

3 rooms, 2 cards, quota 12 / max_lost 10. Search cost: 293 runs, 35 s wall.

**Class driven by:** legal-plan win rate 44.44% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 0.8% (135 legal of 16000) |
| **legal-plan win rate** (freer sampler) | **44.44%** (60/135 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 1 |
| `K` EXHAUSTIVE (ground truth) | 3  — 1296 genomes enumerated (stops<=3), 3 distinct served-room structures, 3 winning (>=1/2 seeds) |
| `e1` (evals to first win) | 4 |
| `skill_gap` (ea@B - random best) | +0.0 |
| ladder steps (budget doublings that beat noise 0.00) | 0 |
| best-EA vs thesis structural distance | 0.67 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +11.2 |

Thesis route-set on TEST seeds: score 1250.70, wins 2/2, served ~12, lost ~0.0.

Ladder (median TEST score vs budget): 50:1246  100:1246  200:1246  400:1246 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  2 cells, serves ["B", "C"]
card 1 line  2 cells, serves ["A", "B"]
```

**Verdict.** SOFT-BORDER: a broad basin — 44.44% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap +0.0 over 0 ladder step(s). The EA trails the hand thesis (-4.8). Exhaustive K = 3.

## R-4 — Bottleneck   [soft-border]

Thesis: *one width-2 corridor, two lifts - only one fits, so they take turns*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 88.00%, 176/200 legal draws; legality 7.8%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 2/2 seeds.

4 rooms, 2 cards, quota 12 / max_lost 8. Search cost: 1560 runs, 16 s wall.

**Class driven by:** legal-plan win rate 88.00% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 7.8% (200 legal of 2559) |
| **legal-plan win rate** (freer sampler) | **88.00%** (176/200 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 60 |
| `K` EXHAUSTIVE (ground truth) | 60  — 1296 genomes enumerated (stops<=3), 66 distinct served-room structures, 60 winning (>=1/2 seeds) |
| `e1` (evals to first win) | 2 |
| `skill_gap` (ea@B - random best) | +0.0 |
| ladder steps (budget doublings that beat noise 0.00) | 1 |
| best-EA vs thesis structural distance | 0.17 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +3.1 |

Thesis route-set on TEST seeds: score 1248.80, wins 2/2, served ~13, lost ~0.0.

Ladder (median TEST score vs budget): 50:1248  100:1248  200:1249*  400:1249 -> **1 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  7 cells, serves ["A", "B"]
card 1 line  9 cells, serves ["B", "C", "D"]
```

**Verdict.** SOFT-BORDER: a broad basin — 88.00% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap +0.0 over 1 ladder step(s). The EA matches the hand thesis (+0.0). Exhaustive K = 60.

## R-5 — Relay   [soft-border]

Thesis: *the building is split in two - one atrium joins them, so lifts must hand off*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 71.00%, 142/200 legal draws; legality 1.4%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 2/2 seeds.

5 rooms, 2 cards, quota 16 / max_lost 12. Search cost: 752 runs, 14 s wall.

**Class driven by:** legal-plan win rate 71.00% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 1.4% (200 legal of 13860) |
| **legal-plan win rate** (freer sampler) | **71.00%** (142/200 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 11 |
| `K` EXHAUSTIVE (ground truth) | 18  — 22500 genomes enumerated (stops<=3), 21 distinct served-room structures, 18 winning (>=1/2 seeds) |
| `e1` (evals to first win) | 2 |
| `skill_gap` (ea@B - random best) | +0.0 |
| ladder steps (budget doublings that beat noise 0.00) | 0 |
| best-EA vs thesis structural distance | 0.17 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | -9.4 |

Thesis route-set on TEST seeds: score 1247.50, wins 2/2, served ~17, lost ~0.0.

Ladder (median TEST score vs budget): 50:1247  100:1247  200:1247  400:1247 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  5 cells, serves ["A", "B", "C"]
card 1 line  3 cells, serves ["C", "D"]
```

**Verdict.** SOFT-BORDER: a broad basin — 71.00% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap +0.0 over 0 ladder step(s). The EA matches the hand thesis (-0.9). Exhaustive K = 18.

## R-6 — Squeeze   [puzzle]

Thesis: *a capped shaft that STEPS across - split it, hand off at the atrium in the bend*

**Classifier:** legal random plans mostly LOSE (legal-plan win rate 10.00%, 20/200 legal draws; legality 2.5% of raw draws are even legal under the caps) but the solver reliably WINS (solvable on 2/2 seeds, first win at 26 evals) — a constraint puzzle whose win requires a specific cooperative structure. (For reference, the spec's raw w_rand rule would say 'soft-border'.)

5 rooms, 2 cards, quota 20 / max_lost 8. Search cost: 1512 runs, 23 s wall.

**Class driven by:** legal-plan win rate 10.00% < 40% (few work) AND solvable 2/2

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 2.5% (200 legal of 8117) |
| **legal-plan win rate** (freer sampler) | **10.00%** (20/200 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 18 |
| `K` EXHAUSTIVE (ground truth) | 11  — 6400 genomes enumerated (stops<=3), 38 distinct served-room structures, 11 winning (>=1/2 seeds) |
| `e1` (evals to first win) | 26 |
| `skill_gap` (ea@B - random best) | +0.0 |
| ladder steps (budget doublings that beat noise 0.00) | 0 |
| best-EA vs thesis structural distance | 0.80 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +4.7 |

Thesis route-set on TEST seeds: score 1252.20, wins 2/2, served ~21, lost ~0.0.

Ladder (median TEST score vs budget): 50:1246  100:1246  200:1246  400:1246 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  8 cells, serves ["C", "D", "E"]
card 1 line  5 cells, serves ["A", "B", "C"]
```

**Verdict.** PUZZLE: only 10.00% of legal random plans win (the overlap cap forces a specific cooperative structure — the freer sampler almost never stumbles into it), but the solver finds a legal win on 2/2 seeds (first win at 26 evals). The best single genome generalises to 2/2 test seeds (a fragile win). Exhaustive K = 11. The intended cooperative split IS among the winning niches.

## R-7 — Express Line   [soft-border]

Thesis: *give the express its own long channel to the penthouse; the local does the office stops*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 77.00%, 154/200 legal draws; legality 7.4%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 2/2 seeds.

5 rooms, 2 cards, quota 16 / max_lost 10. Search cost: 1852 runs, 44 s wall.

**Class driven by:** legal-plan win rate 77.00% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 7.4% (200 legal of 2716) |
| **legal-plan win rate** (freer sampler) | **77.00%** (154/200 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 129 |
| `K` EXHAUSTIVE (ground truth) | 170  — 22500 genomes enumerated (stops<=3), 210 distinct served-room structures, 170 winning (>=1/2 seeds) |
| `e1` (evals to first win) | 2 |
| `skill_gap` (ea@B - random best) | +0.3 |
| ladder steps (budget doublings that beat noise 0.00) | 1 |
| best-EA vs thesis structural distance | 0.70 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +4.2 |

Thesis route-set on TEST seeds: score 1238.60, wins 2/2, served ~17, lost ~0.0.

Ladder (median TEST score vs budget): 50:1240  100:1240  200:1240  400:1243* -> **1 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line 14 cells, serves ["A", "B", "C", "D", "E"]
card 1 line 11 cells, serves ["A", "B"]
```

**Verdict.** SOFT-BORDER: a broad basin — 77.00% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap +0.3 over 1 ladder step(s). The EA beats the hand thesis (+4.3). Exhaustive K = 170.

## R-8 — Freight   [soft-border]

Thesis: *wide delivery men wheel carts to the cafe - only the cargo lift fits them; commuters take the local*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 53.50%, 107/200 legal draws; legality 9.8%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 2/2 seeds.

5 rooms, 2 cards, quota 15 / max_lost 8. Search cost: 1570 runs, 21 s wall.

**Class driven by:** legal-plan win rate 53.50% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 9.8% (200 legal of 2034) |
| **legal-plan win rate** (freer sampler) | **53.50%** (107/200 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 101 |
| `K` EXHAUSTIVE (ground truth) | n/a — space too large (810^2 = 656100 genomes > cap 60000); archive estimate used instead |
| `e1` (evals to first win) | 2 |
| `skill_gap` (ea@B - random best) | +13.4 |
| ladder steps (budget doublings that beat noise 0.00) | 0 |
| best-EA vs thesis structural distance | 0.20 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +6.8 |

Thesis route-set on TEST seeds: score 1221.90, wins 2/2, served ~15, lost ~0.0.

Ladder (median TEST score vs budget): 50:1230  100:1230  200:1229  400:1229 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line 10 cells, serves ["A", "B", "C", "E"]
card 1 line  8 cells, serves ["A", "B", "C", "D", "E"]
```

**Verdict.** SOFT-BORDER: a broad basin — 53.50% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap +13.4 over 0 ladder step(s). The EA beats the hand thesis (+7.5). Exhaustive K n/a (space too large; archive K = 101).

## R-9 — Cross-Dock   [puzzle]

Thesis: *each store feeds only its own cafe - pair them up with two non-overlapping snakes*

**Classifier:** legal random plans mostly LOSE (legal-plan win rate 19.50%, 39/200 legal draws; legality 4.3% of raw draws are even legal under the caps) but the solver reliably WINS (solvable on 2/2 seeds, first win at 14 evals) — a constraint puzzle whose win requires a specific cooperative structure. (For reference, the spec's raw w_rand rule would say 'soft-border'.)

10 rooms, 2 cards, quota 15 / max_lost 10. Search cost: 1430 runs, 34 s wall.

**Class driven by:** legal-plan win rate 19.50% < 40% (few work) AND solvable 2/2

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 4.3% (200 legal of 4617) |
| **legal-plan win rate** (freer sampler) | **19.50%** (39/200 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 121 |
| `K` EXHAUSTIVE (ground truth) | n/a — space too large (810^2 = 656100 genomes > cap 60000); archive estimate used instead |
| `e1` (evals to first win) | 14 |
| `skill_gap` (ea@B - random best) | +8.9 |
| ladder steps (budget doublings that beat noise 0.00) | 2 |
| best-EA vs thesis structural distance | 0.38 |
| intended solution among winning niches | no |
| seed fragility (train - test) | +0.1 |

Thesis route-set on TEST seeds: score 1236.40, wins 2/2, served ~17, lost ~0.0.

Ladder (median TEST score vs budget): 50:1192  100:1192  200:1225*  400:1231* -> **2 step(s)**.

Best route-set found (served rooms per card):

```
card 0 LOOP 20 cells, serves ["A", "B", "C", "D", "E", "F", "G", "H"]
card 1 line  8 cells, serves ["I", "J"]
```

**Verdict.** PUZZLE: only 19.50% of legal random plans win (the overlap cap forces a specific cooperative structure — the freer sampler almost never stumbles into it), but the solver finds a legal win on 2/2 seeds (first win at 14 evals). The best single genome generalises to 2/2 test seeds (a fragile win). Exhaustive K n/a (space too large; archive K = 121). The intended split was NOT recorded among niches.

## R-10 — Rounds   [soft-border]

Thesis: *residents keep cycling home->work->cafe - the return trips are the real load*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 96.00%, 192/200 legal draws; legality 16.3%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 2/2 seeds.

4 rooms, 2 cards, quota 20 / max_lost 12. Search cost: 1554 runs, 17 s wall.

**Class driven by:** legal-plan win rate 96.00% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 16.3% (200 legal of 1224) |
| **legal-plan win rate** (freer sampler) | **96.00%** (192/200 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 47 |
| `K` EXHAUSTIVE (ground truth) | 41  — 1296 genomes enumerated (stops<=3), 45 distinct served-room structures, 41 winning (>=1/2 seeds) |
| `e1` (evals to first win) | 2 |
| `skill_gap` (ea@B - random best) | -0.5 |
| ladder steps (budget doublings that beat noise 0.00) | 1 |
| best-EA vs thesis structural distance | 0.25 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +6.0 |

Thesis route-set on TEST seeds: score 1229.80, wins 2/2, served ~20, lost ~0.0.

Ladder (median TEST score vs budget): 50:1240  100:1244*  200:1244  400:1241 -> **1 step(s)**.

Best route-set found (served rooms per card):

```
card 0 LOOP 10 cells, serves ["A", "B", "C", "D"]
card 1 line 10 cells, serves ["A", "B", "C", "D"]
```

**Verdict.** SOFT-BORDER: a broad basin — 96.00% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap -0.5 over 1 ladder step(s). The EA beats the hand thesis (+11.2). Exhaustive K = 41.

## Method caveats / what I don't fully trust

- **Solver-relative solvability.** "Solvable" is per-seed: for >=7/8 TEST seeds, at least one route-set the search discovered as a winner (a niche example, the best genome, or the hand thesis) wins that seed within the budget. It is NOT "one genome wins every seed" — R-6's cooperative split is a legal win but seed-fragile (the best single genome generalises to only 5/8 test seeds), so requiring one genome to win 7/8 would falsely read R-6 as unsolvable. A false BROKEN (our decoder missed a real win) and a false GOOD PUZZLE (the solver found a path no human would) are the two states this most easily confuses (docs/v5-transition-spec.md §6). The numbers are a filter, never the verdict.
- **The thesis is used as a known-legal winner in the solvability check.** That makes solvability not strictly solver-only for the shipped levels; the per-seed count would be re-derived from solver-found winners alone before this gates NEW (un-thesised) levels.
- **K is a secondary quality signal, not the classifier's discriminator.** The archive `K` (distinct `win_signature`s the EA evaluated) does NOT separate puzzle from soft on this tiny suite — signature granularity (overlap-cell counts, loop flags) can give a puzzle a HIGHER archive K than a tutorial. Where the capped stop-sequence space is small enough, **exhaustive K** enumerates all winning per-card served-room structures (ground truth, Sturtevant EPCG); where the space is too large (R-4, with 8 docks x 3 cards) it falls back to the archive estimate, stated as such. Exhaustive K enumerates dock-stop sequences up to length 3 and card-decode-order permutations, so a winner reachable only by a longer sequence would be missed — a known blind spot on the enumerated levels.
- **The freer sampler's legality rate conflates two things**: how constraining the caps are AND how often a drifted self-avoiding walk dead-ends or misses a room on a given geometry. So a low legality rate (e.g. R-4 at ~2%%, which has NO caps) is not by itself evidence of a constraint puzzle — the classifier reads the legal-plan WIN rate, not legality, for exactly this reason.
- **e1 is a within-level relative difficulty**, evals-to-first-win for THIS solver — not human difficulty (Sturtevant 2020).
- The decoder is order-dependent under the overlap cap (card 0 claims capacity first); a reorder-cards mutation lets the search rearrange it, but a solution only reachable by a decode order the mutation never tried could read as harder than it is.
