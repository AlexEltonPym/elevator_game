# v5 depth report — GENERATED by tools/v5/run_depth5.gd

Do not hand-edit: re-run the depth tool instead. Generated 2026-08-14T11:17:25  **--quick mode: tiny budgets + 2 seeds, indicative only**.

## How to read this

- ONE search pass per level yields TWO readings (docs/v5-transition-spec.md §2.2): Reading A (soft-border) = the random-plan win basin + `skill_gap` + the ladder; Reading B (puzzle) = solvability + `K` + `e1`. A **balance-keyed classifier** picks the headline.
- **The random baseline uses a FREER sampler** (drifted self-avoiding walks between random dock stops), NOT the smart shortest-path decoder. The smart decoder auto-covers intermediate rooms and auto-threads a level's cooperative split, so a "random" gene would do the puzzle's reasoning for free and inflate a constraint level's random-win. The freer sampler produces genuinely varied legal plans that often miss rooms and mis-thread the shaft, so its win rate reflects the LEVEL's difficulty, not the decoder's cleverness. (The EA / greedy keep the smart decoder.)
- **Two probe numbers.** *legality rate* = fraction of raw random draws that are even legal (all cards decode under the caps, serve >= 2 rooms); it reflects caps AND the wandering sampler's dead-end rate, so read it as a rough constraint indicator, not a pure cap measure. *legal-plan win rate* = wins among legal draws — **this is the difficulty signal that drives the classifier.**
- **Classifier (balance-keyed):** legal-plan win rate >= 40% (a broad basin of working plans) -> **soft-border**; legal-plan win rate < 40% (few legal plans work) AND the solver is solvable >=7/8 seeds -> **puzzle**; < 40% AND not solvable -> **BROKEN**. The 40% bar is the spec's own forgiving/tutorial bar (§4.1). The `spec-primary` column shows the spec §2.3 raw `w_rand < 5` rule for audit only; it is NOT the driver.
- **Solvability is per-seed** (spec §2.2a): for >= 7/8 TEST seeds, does the solver's set of discovered winning route-sets contain one that wins that seed? (Not "does one genome win every seed" — the right question when a legal win is seed-fragile.)
- **`K` is a secondary quality signal, not the discriminator.** `K (arch)` is the archive estimate (distinct winning `win_signature`s the EA evaluated). `K (exh)` is EXHAUSTIVE ground truth where the capped stop-sequence space is small enough to enumerate (distinct winning per-card served-room structures); `--` means the space was too large and only the archive estimate is available.
- One budget unit = one sim run of THE REAL v5 LEVEL (shipped quota / max_lost / win-lose live, 300 game-second timeout). `WIN -> 1000 + (300 - t_win)`; else `served - 3.0*lost - 0.01*avg_wait`. Optimizers search TRAIN seeds at STEP 0.25; every reported number is a median over the disjoint TEST seeds at STEP 0.10. Solvability is always **relative to this solver at this budget** (Ropossum's caveat): "winnable by our EA within 800 evals on these seeds", never "winnable".

## Classification table

Class is driven by the **legal-plan win rate** (freer sampler) + solvability. `spec-primary` is the raw spec §2.3 `w_rand` rule shown for audit only.

| level | class | legality | legal-plan win rate | solvable | K (arch) | K (exh) | e1 evals | spec-primary |
|---|---|---|---|---|---|---|---|---|
| **R-1** One Lift | **soft-border** | 60.2% | 77.00% (154/200) | 2/2 | 4 | 3 | 2 | soft-border |
| **R-2** Double Duty | **soft-border** | 70.4% | 100.00% (200/200) | 2/2 | 1 | 1 | 2 | soft-border |
| **R-3** Crossing | **soft-border** | 19.1% | 100.00% (200/200) | 2/2 | 18 | 6 | 2 | soft-border |
| **R-4** Big Store | **soft-border** | 2.0% | 88.50% (177/200) | 2/2 | 360 | — | 2 | soft-border |
| **R-5** Bottleneck | **soft-border** | 8.1% | 86.50% (173/200) | 2/2 | 59 | 60 | 2 | soft-border |
| **R-6** Squeeze | **puzzle** | 3.0% | 3.00% (6/200) | 2/2 | 8 | 4 | 14 | soft-border ⚠ |
| **R-7** Two Towers | **soft-border** | 23.3% | 96.00% (192/200) | 2/2 | 103 | 42 | 2 | soft-border |
| **R-8** The Narrows | **soft-border** | 45.6% | 78.00% (156/200) | 2/2 | 2 | 3 | 2 | soft-border |
| **R-9** Relay | **soft-border** | 1.4% | 62.00% (124/200) | 2/2 | 11 | 18 | 2 | soft-border |
| **R-10** Threadneedle | **soft-border** | 1.3% | 60.50% (121/200) | 2/2 | 6 | 5 | 2 | soft-border |
| **R-11** Express Run | **soft-border** | 15.3% | 86.00% (172/200) | 2/2 | 68 | 30 | 2 | soft-border |
| **R-12** Freight | **soft-border** | 3.6% | 76.50% (153/200) | 2/2 | 18 | 4 | 2 | soft-border |
| **R-13** Regulars | **soft-border** | 14.3% | 83.00% (166/200) | 2/2 | 153 | 163 | 2 | soft-border |
| **R-14** Interchange | **soft-border** | 1.7% | 94.50% (189/200) | 2/2 | 315 | — | 2 | soft-border |

## Validation of the classifier against hand-understood levels

- **R-1 "One Lift" expected SOFT-BORDER** — got **soft-border**, driven by: legal-plan win rate 77.00% >= 40% (broad basin). CORRECT via the balance signal (not a brittle threshold): a broad basin of legal random plans win.
- **R-6 "Squeeze" expected PUZZLE** — got **puzzle**, driven by: legal-plan win rate 3.00% < 40% (few work) AND solvable 2/2. CORRECT via the balance signal. The FREER sampler drops R-6's legal-plan win rate to 3.00% (the smart-decoder version had inflated it to ~6%), toward the ~0 the design intended — genuinely-random legal plans almost never solve it, but the solver is solvable 2/2 seeds via the cooperative split, and the intended split IS among the winning niches. Exhaustive K = 4 confirms the winning structure is small.

Both hand-understood levels are now called correctly by the win/loss BALANCE of legal random plans — a signal with wide margin (R-6 ~3% vs the tutorials' 77-100%), not a knife-edge count threshold.

## R-1 — One Lift   [soft-border]

Thesis: *one lift, three rooms - draw a line past them all*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 77.00%, 154/200 legal draws; legality 60.2%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 2/2 seeds.

3 rooms, 1 cards, quota 10 / max_lost 8. Search cost: 272 runs, 4 s wall.

**Class driven by:** legal-plan win rate 77.00% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 60.2% (200 legal of 332) |
| **legal-plan win rate** (freer sampler) | **77.00%** (154/200 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 4 |
| `K` EXHAUSTIVE (ground truth) | 3  — 12 genomes enumerated (stops<=3), 3 distinct served-room structures, 3 winning (>=1/2 seeds) |
| `e1` (evals to first win) | 2 |
| `skill_gap` (ea@B - random best) | +0.0 |
| ladder steps (budget doublings that beat noise 0.00) | 0 |
| best-EA vs thesis structural distance | 0.00 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +4.5 |

Thesis route-set on TEST seeds: score 1248.50, wins 2/2, served ~11, lost ~0.0.

Ladder (median TEST score vs budget): 50:1250  100:1250  200:1250  400:1250 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 LOOP 14 cells, serves ["A", "B", "C"]
```

**Verdict.** SOFT-BORDER: a broad basin — 77.00% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap +0.0 over 0 ladder step(s). The EA beats the hand thesis (+1.4). Exhaustive K = 3.

## R-2 — Double Duty   [soft-border]

Thesis: *one column can serve two rooms at once - find the cell that touches both*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 100.00%, 200/200 legal draws; legality 70.4%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 2/2 seeds.

3 rooms, 1 cards, quota 12 / max_lost 8. Search cost: 232 runs, 2 s wall.

**Class driven by:** legal-plan win rate 100.00% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 70.4% (200 legal of 284) |
| **legal-plan win rate** (freer sampler) | **100.00%** (200/200 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 1 |
| `K` EXHAUSTIVE (ground truth) | 1  — 2 genomes enumerated (stops<=3), 1 distinct served-room structures, 1 winning (>=1/2 seeds) |
| `e1` (evals to first win) | 2 |
| `skill_gap` (ea@B - random best) | +0.0 |
| ladder steps (budget doublings that beat noise 0.00) | 0 |
| best-EA vs thesis structural distance | 0.00 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +5.7 |

Thesis route-set on TEST seeds: score 1251.40, wins 2/2, served ~13, lost ~0.0.

Ladder (median TEST score vs budget): 50:1251  100:1251  200:1251  400:1251 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  5 cells, serves ["A", "B", "C"]
```

**Verdict.** SOFT-BORDER: a broad basin — 100.00% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap +0.0 over 0 ladder step(s). The EA matches the hand thesis (+0.0). Exhaustive K = 1.

## R-3 — Crossing   [soft-border]

Thesis: *two lifts share the atrium - ride in, cross over, ride on*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 100.00%, 200/200 legal draws; legality 19.1%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 2/2 seeds.

3 rooms, 2 cards, quota 24 / max_lost 400. Search cost: 1448 runs, 37 s wall.

**Class driven by:** legal-plan win rate 100.00% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 19.1% (200 legal of 1048) |
| **legal-plan win rate** (freer sampler) | **100.00%** (200/200 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 18 |
| `K` EXHAUSTIVE (ground truth) | 6  — 1296 genomes enumerated (stops<=3), 6 distinct served-room structures, 6 winning (>=1/2 seeds) |
| `e1` (evals to first win) | 2 |
| `skill_gap` (ea@B - random best) | +3.7 |
| ladder steps (budget doublings that beat noise 0.00) | 1 |
| best-EA vs thesis structural distance | 0.33 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +1.1 |

Thesis route-set on TEST seeds: score 1253.70, wins 2/2, served ~26, lost ~0.0.

Ladder (median TEST score vs budget): 50:1257  100:1259*  200:1259  400:1259 -> **1 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  9 cells, serves ["A", "B", "C"]
card 1 line  9 cells, serves ["A", "B", "C"]
```

**Verdict.** SOFT-BORDER: a broad basin — 100.00% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap +3.7 over 1 ladder step(s). The EA beats the hand thesis (+5.1). Exhaustive K = 6.

## R-4 — Big Store   [soft-border]

Thesis: *a department store, offices and a cafe - lots of empty grid, route it your way*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 88.50%, 177/200 legal draws; legality 2.0%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 2/2 seeds.

5 rooms, 3 cards, quota 18 / max_lost 10. Search cost: 1458 runs, 39 s wall.

**Class driven by:** legal-plan win rate 88.50% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 2.0% (200 legal of 9957) |
| **legal-plan win rate** (freer sampler) | **88.50%** (177/200 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 360 |
| `K` EXHAUSTIVE (ground truth) | n/a — space too large (392^3 = 60236288 genomes > cap 60000); archive estimate used instead |
| `e1` (evals to first win) | 2 |
| `skill_gap` (ea@B - random best) | +12.4 |
| ladder steps (budget doublings that beat noise 0.00) | 2 |
| best-EA vs thesis structural distance | 0.00 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | -6.6 |

Thesis route-set on TEST seeds: score 1243.90, wins 2/2, served ~18, lost ~0.0.

Ladder (median TEST score vs budget): 50:1231  100:1240*  200:1233  400:1246* -> **2 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  3 cells, serves ["A", "B"]
card 1 line 10 cells, serves ["B", "C", "E"]
card 2 line  7 cells, serves ["B", "D"]
```

**Verdict.** SOFT-BORDER: a broad basin — 88.50% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap +12.4 over 2 ladder step(s). The EA beats the hand thesis (+1.6). Exhaustive K n/a (space too large; archive K = 360).

## R-5 — Bottleneck   [soft-border]

Thesis: *one width-2 corridor, two lifts - only one fits, so they take turns*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 86.50%, 173/200 legal draws; legality 8.1%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 2/2 seeds.

4 rooms, 2 cards, quota 12 / max_lost 8. Search cost: 1560 runs, 21 s wall.

**Class driven by:** legal-plan win rate 86.50% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 8.1% (200 legal of 2465) |
| **legal-plan win rate** (freer sampler) | **86.50%** (173/200 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 59 |
| `K` EXHAUSTIVE (ground truth) | 60  — 1296 genomes enumerated (stops<=3), 66 distinct served-room structures, 60 winning (>=1/2 seeds) |
| `e1` (evals to first win) | 2 |
| `skill_gap` (ea@B - random best) | +0.0 |
| ladder steps (budget doublings that beat noise 0.00) | 1 |
| best-EA vs thesis structural distance | 0.17 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +3.1 |

Thesis route-set on TEST seeds: score 1248.80, wins 2/2, served ~13, lost ~0.0.

Ladder (median TEST score vs budget): 50:1248  100:1249*  200:1249  400:1249 -> **1 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  7 cells, serves ["A", "B"]
card 1 line  9 cells, serves ["B", "C", "D"]
```

**Verdict.** SOFT-BORDER: a broad basin — 86.50% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap +0.0 over 1 ladder step(s). The EA matches the hand thesis (+0.0). Exhaustive K = 60.

## R-6 — Squeeze   [puzzle]

Thesis: *a capped shared shaft - two lifts must SPLIT it and hand off at the transfer*

**Classifier:** legal random plans mostly LOSE (legal-plan win rate 3.00%, 6/200 legal draws; legality 3.0% of raw draws are even legal under the caps) but the solver reliably WINS (solvable on 2/2 seeds, first win at 14 evals) — a constraint puzzle whose win requires a specific cooperative structure. (For reference, the spec's raw w_rand rule would say 'soft-border'.)

5 rooms, 2 cards, quota 24 / max_lost 8. Search cost: 1501 runs, 27 s wall.

**Class driven by:** legal-plan win rate 3.00% < 40% (few work) AND solvable 2/2

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 3.0% (200 legal of 6703) |
| **legal-plan win rate** (freer sampler) | **3.00%** (6/200 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 8 |
| `K` EXHAUSTIVE (ground truth) | 4  — 6400 genomes enumerated (stops<=3), 34 distinct served-room structures, 4 winning (>=1/2 seeds) |
| `e1` (evals to first win) | 14 |
| `skill_gap` (ea@B - random best) | +0.0 |
| ladder steps (budget doublings that beat noise 0.00) | 1 |
| best-EA vs thesis structural distance | 0.80 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +1.2 |

Thesis route-set on TEST seeds: score -3.15, wins 1/2, served ~23, lost ~5.5.

Ladder (median TEST score vs budget): 50:-3  100:1244*  200:1244  400:1244 -> **1 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  6 cells, serves ["C", "D", "E"]
card 1 line  6 cells, serves ["A", "B", "E"]
```

**Verdict.** PUZZLE: only 3.00% of legal random plans win (the overlap cap forces a specific cooperative structure — the freer sampler almost never stumbles into it), but the solver finds a legal win on 2/2 seeds (first win at 14 evals). The best single genome generalises to 2/2 test seeds (a fragile win). Exhaustive K = 4. The intended cooperative split IS among the winning niches.

## R-7 — Two Towers   [soft-border]

Thesis: *two lifts, two stacks - give each lift its own half of the building*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 96.00%, 192/200 legal draws; legality 23.3%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 2/2 seeds.

4 rooms, 2 cards, quota 12 / max_lost 10. Search cost: 1584 runs, 21 s wall.

**Class driven by:** legal-plan win rate 96.00% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 23.3% (200 legal of 860) |
| **legal-plan win rate** (freer sampler) | **96.00%** (192/200 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 103 |
| `K` EXHAUSTIVE (ground truth) | 42  — 1296 genomes enumerated (stops<=3), 45 distinct served-room structures, 42 winning (>=1/2 seeds) |
| `e1` (evals to first win) | 2 |
| `skill_gap` (ea@B - random best) | -0.4 |
| ladder steps (budget doublings that beat noise 0.00) | 0 |
| best-EA vs thesis structural distance | 0.00 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +3.5 |

Thesis route-set on TEST seeds: score 1252.90, wins 2/2, served ~13, lost ~0.0.

Ladder (median TEST score vs budget): 50:1253  100:1253  200:1253  400:1253 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  6 cells, serves ["A", "B"]
card 1 line  6 cells, serves ["C", "D"]
```

**Verdict.** SOFT-BORDER: a broad basin — 96.00% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap -0.4 over 0 ladder step(s). The EA matches the hand thesis (+0.0). Exhaustive K = 42.

## R-8 — The Narrows   [soft-border]

Thesis: *one striped corridor - a normal lift threads it to reach the far side*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 78.00%, 156/200 legal draws; legality 45.6%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 2/2 seeds.

3 rooms, 1 cards, quota 10 / max_lost 8. Search cost: 252 runs, 4 s wall.

**Class driven by:** legal-plan win rate 78.00% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 45.6% (200 legal of 439) |
| **legal-plan win rate** (freer sampler) | **78.00%** (156/200 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 2 |
| `K` EXHAUSTIVE (ground truth) | 3  — 12 genomes enumerated (stops<=3), 3 distinct served-room structures, 3 winning (>=1/2 seeds) |
| `e1` (evals to first win) | 2 |
| `skill_gap` (ea@B - random best) | +0.0 |
| ladder steps (budget doublings that beat noise 0.00) | 0 |
| best-EA vs thesis structural distance | 0.00 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | -1.0 |

Thesis route-set on TEST seeds: score 1257.70, wins 2/2, served ~10, lost ~0.0.

Ladder (median TEST score vs budget): 50:1258  100:1258  200:1258  400:1258 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  7 cells, serves ["A", "B", "C"]
```

**Verdict.** SOFT-BORDER: a broad basin — 78.00% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap +0.0 over 0 ladder step(s). The EA matches the hand thesis (+0.0). Exhaustive K = 3.

## R-9 — Relay   [soft-border]

Thesis: *the building is split in two - one atrium joins them, so lifts must hand off*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 62.00%, 124/200 legal draws; legality 1.4%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 2/2 seeds.

5 rooms, 2 cards, quota 16 / max_lost 12. Search cost: 760 runs, 20 s wall.

**Class driven by:** legal-plan win rate 62.00% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 1.4% (200 legal of 14748) |
| **legal-plan win rate** (freer sampler) | **62.00%** (124/200 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 11 |
| `K` EXHAUSTIVE (ground truth) | 18  — 22500 genomes enumerated (stops<=3), 21 distinct served-room structures, 18 winning (>=1/2 seeds) |
| `e1` (evals to first win) | 2 |
| `skill_gap` (ea@B - random best) | +0.0 |
| ladder steps (budget doublings that beat noise 0.00) | 0 |
| best-EA vs thesis structural distance | 0.78 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | -3.6 |

Thesis route-set on TEST seeds: score 1243.10, wins 2/2, served ~17, lost ~0.0.

Ladder (median TEST score vs budget): 50:1245  100:1245  200:1245  400:1245 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  3 cells, serves ["C", "D"]
card 1 line  5 cells, serves ["A", "B", "C"]
```

**Verdict.** SOFT-BORDER: a broad basin — 62.00% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap +0.0 over 0 ladder step(s). The EA beats the hand thesis (+1.6). Exhaustive K = 18.

## R-10 — Threadneedle   [soft-border]

Thesis: *a capped zig-zag shaft - split it and meet only at the transfer*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 60.50%, 121/200 legal draws; legality 1.3%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 2/2 seeds.

5 rooms, 2 cards, quota 20 / max_lost 10. Search cost: 499 runs, 103 s wall.

**Class driven by:** legal-plan win rate 60.50% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 1.3% (200 legal of 15984) |
| **legal-plan win rate** (freer sampler) | **60.50%** (121/200 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 6 |
| `K` EXHAUSTIVE (ground truth) | 5  — 6400 genomes enumerated (stops<=3), 9 distinct served-room structures, 5 winning (>=1/2 seeds) |
| `e1` (evals to first win) | 2 |
| `skill_gap` (ea@B - random best) | +0.0 |
| ladder steps (budget doublings that beat noise 0.00) | 0 |
| best-EA vs thesis structural distance | 0.17 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +4.5 |

Thesis route-set on TEST seeds: score 1251.80, wins 2/2, served ~22, lost ~0.0.

Ladder (median TEST score vs budget): 50:1253  100:1253  200:1253  400:1253 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  5 cells, serves ["A", "B", "C"]
card 1 line  3 cells, serves ["C", "D"]
```

**Verdict.** SOFT-BORDER: a broad basin — 60.50% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap +0.0 over 0 ladder step(s). The EA beats the hand thesis (+1.3). Exhaustive K = 5.

## R-11 — Express Run   [soft-border]

Thesis: *a tall shaft - the express (double speed) earns its keep on the long haul*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 86.00%, 172/200 legal draws; legality 15.3%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 2/2 seeds.

4 rooms, 2 cards, quota 16 / max_lost 10. Search cost: 1514 runs, 21 s wall.

**Class driven by:** legal-plan win rate 86.00% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 15.3% (200 legal of 1305) |
| **legal-plan win rate** (freer sampler) | **86.00%** (172/200 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 68 |
| `K` EXHAUSTIVE (ground truth) | 30  — 1296 genomes enumerated (stops<=3), 36 distinct served-room structures, 30 winning (>=1/2 seeds) |
| `e1` (evals to first win) | 2 |
| `skill_gap` (ea@B - random best) | -7.7 |
| ladder steps (budget doublings that beat noise 0.00) | 0 |
| best-EA vs thesis structural distance | 0.50 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +6.9 |

Thesis route-set on TEST seeds: score 1237.70, wins 2/2, served ~17, lost ~0.0.

Ladder (median TEST score vs budget): 50:1240  100:1240  200:1240  400:1240 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  3 cells, serves ["A", "B"]
card 1 LOOP 22 cells, serves ["A", "B", "C", "D"]
```

**Verdict.** SOFT-BORDER: a broad basin — 86.00% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap -7.7 over 0 ladder step(s). The EA beats the hand thesis (+2.5). Exhaustive K = 30.

## R-12 — Freight   [soft-border]

Thesis: *a wide cargo lift can't fit the narrow corridor - so it works the store, the local relays*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 76.50%, 153/200 legal draws; legality 3.6%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 2/2 seeds.

4 rooms, 2 cards, quota 18 / max_lost 12. Search cost: 1472 runs, 20 s wall.

**Class driven by:** legal-plan win rate 76.50% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 3.6% (200 legal of 5549) |
| **legal-plan win rate** (freer sampler) | **76.50%** (153/200 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 18 |
| `K` EXHAUSTIVE (ground truth) | 4  — 6400 genomes enumerated (stops<=3), 15 distinct served-room structures, 4 winning (>=1/2 seeds) |
| `e1` (evals to first win) | 2 |
| `skill_gap` (ea@B - random best) | +0.0 |
| ladder steps (budget doublings that beat noise 0.00) | 0 |
| best-EA vs thesis structural distance | 0.25 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +7.0 |

Thesis route-set on TEST seeds: score 1231.80, wins 2/2, served ~19, lost ~0.0.

Ladder (median TEST score vs budget): 50:1237  100:1237  200:1237  400:1237 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  2 cells, serves ["A", "B"]
card 1 line  9 cells, serves ["A", "C", "D"]
```

**Verdict.** SOFT-BORDER: a broad basin — 76.50% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap +0.0 over 0 ladder step(s). The EA beats the hand thesis (+5.5). Exhaustive K = 4.

## R-13 — Regulars   [soft-border]

Thesis: *regulars keep coming back - a plan tuned for one trip drowns under the return trips*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 83.00%, 166/200 legal draws; legality 14.3%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 2/2 seeds.

5 rooms, 2 cards, quota 20 / max_lost 12. Search cost: 1840 runs, 47 s wall.

**Class driven by:** legal-plan win rate 83.00% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 14.3% (200 legal of 1399) |
| **legal-plan win rate** (freer sampler) | **83.00%** (166/200 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 153 |
| `K` EXHAUSTIVE (ground truth) | 163  — 6400 genomes enumerated (stops<=3), 171 distinct served-room structures, 163 winning (>=1/2 seeds) |
| `e1` (evals to first win) | 2 |
| `skill_gap` (ea@B - random best) | +6.9 |
| ladder steps (budget doublings that beat noise 0.00) | 2 |
| best-EA vs thesis structural distance | 0.25 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +2.7 |

Thesis route-set on TEST seeds: score 1236.40, wins 2/2, served ~20, lost ~0.0.

Ladder (median TEST score vs budget): 50:1236  100:1236  200:1236*  400:1241* -> **2 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line 15 cells, serves ["A", "B", "C", "E"]
card 1 line  7 cells, serves ["B", "C", "D", "E"]
```

**Verdict.** SOFT-BORDER: a broad basin — 83.00% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap +6.9 over 2 ladder step(s). The EA beats the hand thesis (+4.1). Exhaustive K = 163.

## R-14 — Interchange   [soft-border]

Thesis: *a cap-4 trunk fits two lifts but not three - who doubles up where is the puzzle*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 94.50%, 189/200 legal draws; legality 1.7%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 2/2 seeds.

5 rooms, 3 cards, quota 22 / max_lost 12. Search cost: 1432 runs, 35 s wall.

**Class driven by:** legal-plan win rate 94.50% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 1.7% (200 legal of 12068) |
| **legal-plan win rate** (freer sampler) | **94.50%** (189/200 legal draws win) |
| solvability (per-seed: solver win exists) | 2/2 -> solvable |
| best single EA genome generalises (wins on TEST) | 2/2 |
| `K` archive estimate | 315 |
| `K` EXHAUSTIVE (ground truth) | n/a — space too large (80^3 = 512000 genomes > cap 60000); archive estimate used instead |
| `e1` (evals to first win) | 2 |
| `skill_gap` (ea@B - random best) | +2.7 |
| ladder steps (budget doublings that beat noise 0.00) | 0 |
| best-EA vs thesis structural distance | 0.28 |
| intended solution among winning niches | no |
| seed fragility (train - test) | +1.7 |

Thesis route-set on TEST seeds: score 1243.70, wins 2/2, served ~23, lost ~0.0.

Ladder (median TEST score vs budget): 50:1247  100:1247  200:1246  400:1246 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line 15 cells, serves ["A", "B", "E"]
card 1 line  6 cells, serves ["C", "D", "E"]
card 2 LOOP 12 cells, serves ["A", "B", "C"]
```

**Verdict.** SOFT-BORDER: a broad basin — 94.50% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap +2.7 over 0 ladder step(s). The EA beats the hand thesis (+1.9). Exhaustive K n/a (space too large; archive K = 315).

## Method caveats / what I don't fully trust

- **Solver-relative solvability.** "Solvable" is per-seed: for >=7/8 TEST seeds, at least one route-set the search discovered as a winner (a niche example, the best genome, or the hand thesis) wins that seed within the budget. It is NOT "one genome wins every seed" — R-6's cooperative split is a legal win but seed-fragile (the best single genome generalises to only 5/8 test seeds), so requiring one genome to win 7/8 would falsely read R-6 as unsolvable. A false BROKEN (our decoder missed a real win) and a false GOOD PUZZLE (the solver found a path no human would) are the two states this most easily confuses (docs/v5-transition-spec.md §6). The numbers are a filter, never the verdict.
- **The thesis is used as a known-legal winner in the solvability check.** That makes solvability not strictly solver-only for the shipped levels; the per-seed count would be re-derived from solver-found winners alone before this gates NEW (un-thesised) levels.
- **K is a secondary quality signal, not the classifier's discriminator.** The archive `K` (distinct `win_signature`s the EA evaluated) does NOT separate puzzle from soft on this tiny suite — signature granularity (overlap-cell counts, loop flags) can give a puzzle a HIGHER archive K than a tutorial. Where the capped stop-sequence space is small enough, **exhaustive K** enumerates all winning per-card served-room structures (ground truth, Sturtevant EPCG); where the space is too large (R-4, with 8 docks x 3 cards) it falls back to the archive estimate, stated as such. Exhaustive K enumerates dock-stop sequences up to length 3 and card-decode-order permutations, so a winner reachable only by a longer sequence would be missed — a known blind spot on the enumerated levels.
- **The freer sampler's legality rate conflates two things**: how constraining the caps are AND how often a drifted self-avoiding walk dead-ends or misses a room on a given geometry. So a low legality rate (e.g. R-4 at ~2%%, which has NO caps) is not by itself evidence of a constraint puzzle — the classifier reads the legal-plan WIN rate, not legality, for exactly this reason.
- **e1 is a within-level relative difficulty**, evals-to-first-win for THIS solver — not human difficulty (Sturtevant 2020).
- The decoder is order-dependent under the overlap cap (card 0 claims capacity first); a reorder-cards mutation lets the search rearrange it, but a solution only reachable by a decode order the mutation never tried could read as harder than it is.
