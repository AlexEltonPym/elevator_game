# v5 depth report — GENERATED by tools/v5/run_depth5.gd

Do not hand-edit: re-run the depth tool instead. Generated 2026-08-14T10:36:57.

## How to read this

- ONE search pass per level yields TWO readings (docs/v5-transition-spec.md §2.2): Reading A (soft-border) = the random-plan win basin + `skill_gap` + the ladder; Reading B (puzzle) = solvability + `K` + `e1`. A **balance-keyed classifier** picks the headline.
- **The random baseline uses a FREER sampler** (drifted self-avoiding walks between random dock stops), NOT the smart shortest-path decoder. The smart decoder auto-covers intermediate rooms and auto-threads a level's cooperative split, so a "random" gene would do the puzzle's reasoning for free and inflate a constraint level's random-win. The freer sampler produces genuinely varied legal plans that often miss rooms and mis-thread the shaft, so its win rate reflects the LEVEL's difficulty, not the decoder's cleverness. (The EA / greedy keep the smart decoder.)
- **Two probe numbers.** *legality rate* = fraction of raw random draws that are even legal (all cards decode under the caps, serve >= 2 rooms); it reflects caps AND the wandering sampler's dead-end rate, so read it as a rough constraint indicator, not a pure cap measure. *legal-plan win rate* = wins among legal draws — **this is the difficulty signal that drives the classifier.**
- **Classifier (balance-keyed):** legal-plan win rate >= 40% (a broad basin of working plans) -> **soft-border**; legal-plan win rate < 40% (few legal plans work) AND the solver is solvable >=7/8 seeds -> **puzzle**; < 40% AND not solvable -> **BROKEN**. The 40% bar is the spec's own forgiving/tutorial bar (§4.1). The `spec-primary` column shows the spec §2.3 raw `w_rand < 5` rule for audit only; it is NOT the driver.
- **Solvability is per-seed** (spec §2.2a): for >= 7/8 TEST seeds, does the solver's set of discovered winning route-sets contain one that wins that seed? (Not "does one genome win every seed" — the right question when a legal win is seed-fragile.)
- **`K` is a secondary quality signal, not the discriminator.** `K (arch)` is the archive estimate (distinct winning `win_signature`s the EA evaluated). `K (exh)` is EXHAUSTIVE ground truth where the capped stop-sequence space is small enough to enumerate (distinct winning per-card served-room structures); `--` means the space was too large and only the archive estimate is available.
- One budget unit = one sim run of THE REAL v5 LEVEL (shipped quota / max_lost / win-lose live, 300 game-second timeout). `WIN -> 1000 + (300 - t_win)`; else `served - 3.0*lost - 0.01*avg_wait`. Optimizers search TRAIN seeds at STEP 0.25; every reported number is a median over the disjoint TEST seeds at STEP 0.10. Solvability is always **relative to this solver at this budget** (Ropossum's caveat): "winnable by our EA within 6400 evals on these seeds", never "winnable".

## Classification table

Class is driven by the **legal-plan win rate** (freer sampler) + solvability. `spec-primary` is the raw spec §2.3 `w_rand` rule shown for audit only.

| level | class | legality | legal-plan win rate | solvable | K (arch) | K (exh) | e1 evals | spec-primary |
|---|---|---|---|---|---|---|---|---|
| **R-1** One Lift | **soft-border** | 58.7% | 76.67% (460/600) | 8/8 | 4 | 2 | 8 | soft-border |
| **R-2** Double Duty | **soft-border** | 68.2% | 100.00% (600/600) | 8/8 | 1 | 1 | 8 | soft-border |
| **R-3** Crossing | **soft-border** | 19.0% | 100.00% (600/600) | 8/8 | 21 | 6 | 8 | soft-border |
| **R-4** Big Store | **soft-border** | 2.1% | 91.33% (548/600) | 8/8 | 729 | — | 8 | soft-border |
| **R-5** Bottleneck | **soft-border** | 8.4% | 87.17% (523/600) | 8/8 | 60 | 60 | 8 | soft-border |
| **R-6** Squeeze | **puzzle** | 2.9% | 2.17% (13/600) | 8/8 | 10 | 1 | 56 | soft-border ⚠ |

## Validation of the classifier against hand-understood levels

- **R-1 "One Lift" expected SOFT-BORDER** — got **soft-border**, driven by: legal-plan win rate 76.67% >= 40% (broad basin). CORRECT via the balance signal (not a brittle threshold): a broad basin of legal random plans win.
- **R-6 "Squeeze" expected PUZZLE** — got **puzzle**, driven by: legal-plan win rate 2.17% < 40% (few work) AND solvable 8/8. CORRECT via the balance signal. The FREER sampler drops R-6's legal-plan win rate to 2.17% (the smart-decoder version had inflated it to ~6%), toward the ~0 the design intended — genuinely-random legal plans almost never solve it, but the solver is solvable 8/8 seeds via the cooperative split, and the intended split IS among the winning niches. Exhaustive K = 1 confirms the winning structure is near-unique.

Both hand-understood levels are now called correctly by the win/loss BALANCE of legal random plans — a signal with wide margin (R-6 ~2% vs the tutorials' 77-100%), not a knife-edge count threshold.

## R-1 — One Lift   [soft-border]

Thesis: *one lift, three rooms - draw a line past them all*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 76.67%, 460/600 legal draws; legality 58.7%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 8/8 seeds.

3 rooms, 1 cards, quota 10 / max_lost 8. Search cost: 896 runs, 7 s wall.

**Class driven by:** legal-plan win rate 76.67% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 58.7% (600 legal of 1023) |
| **legal-plan win rate** (freer sampler) | **76.67%** (460/600 legal draws win) |
| solvability (per-seed: solver win exists) | 8/8 -> solvable |
| best single EA genome generalises (wins on TEST) | 8/8 |
| `K` archive estimate | 4 |
| `K` EXHAUSTIVE (ground truth) | 2  — 12 genomes enumerated (stops<=3), 3 distinct served-room structures, 2 winning (>=4/8 seeds) |
| `e1` (evals to first win) | 8 |
| `skill_gap` (ea@B - random best) | +0.0 |
| ladder steps (budget doublings that beat noise 1.10) | 0 |
| best-EA vs thesis structural distance | 0.00 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +2.4 |

Thesis route-set on TEST seeds: score 1255.20, wins 8/8, served ~11, lost ~0.0.

Ladder (median TEST score vs budget): 100:1255  400:1255  1600:1255  6400:1255 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  7 cells, serves ["A", "B", "C"]
```

**Verdict.** SOFT-BORDER: a broad basin — 76.67% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap +0.0 over 0 ladder step(s). The EA matches the hand thesis (+0.0). Exhaustive K = 2.

## R-2 — Double Duty   [soft-border]

Thesis: *one column can serve two rooms at once - find the cell that touches both*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 100.00%, 600/600 legal draws; legality 68.2%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 8/8 seeds.

3 rooms, 1 cards, quota 12 / max_lost 8. Search cost: 730 runs, 3 s wall.

**Class driven by:** legal-plan win rate 100.00% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 68.2% (600 legal of 880) |
| **legal-plan win rate** (freer sampler) | **100.00%** (600/600 legal draws win) |
| solvability (per-seed: solver win exists) | 8/8 -> solvable |
| best single EA genome generalises (wins on TEST) | 8/8 |
| `K` archive estimate | 1 |
| `K` EXHAUSTIVE (ground truth) | 1  — 2 genomes enumerated (stops<=3), 1 distinct served-room structures, 1 winning (>=4/8 seeds) |
| `e1` (evals to first win) | 8 |
| `skill_gap` (ea@B - random best) | +0.0 |
| ladder steps (budget doublings that beat noise 0.35) | 0 |
| best-EA vs thesis structural distance | 0.00 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +0.8 |

Thesis route-set on TEST seeds: score 1257.10, wins 8/8, served ~13, lost ~0.0.

Ladder (median TEST score vs budget): 100:1257  400:1257  1600:1257  6400:1257 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  5 cells, serves ["A", "B", "C"]
```

**Verdict.** SOFT-BORDER: a broad basin — 100.00% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap +0.0 over 0 ladder step(s). The EA matches the hand thesis (+0.0). Exhaustive K = 1.

## R-3 — Crossing   [soft-border]

Thesis: *two lifts share the atrium - ride in, cross over, ride on*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 100.00%, 600/600 legal draws; legality 19.0%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 8/8 seeds.

3 rooms, 2 cards, quota 24 / max_lost 400. Search cost: 13564 runs, 402 s wall.

**Class driven by:** legal-plan win rate 100.00% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 19.0% (600 legal of 3155) |
| **legal-plan win rate** (freer sampler) | **100.00%** (600/600 legal draws win) |
| solvability (per-seed: solver win exists) | 8/8 -> solvable |
| best single EA genome generalises (wins on TEST) | 8/8 |
| `K` archive estimate | 21 |
| `K` EXHAUSTIVE (ground truth) | 6  — 1296 genomes enumerated (stops<=3), 6 distinct served-room structures, 6 winning (>=4/8 seeds) |
| `e1` (evals to first win) | 8 |
| `skill_gap` (ea@B - random best) | +0.3 |
| ladder steps (budget doublings that beat noise 0.42) | 0 |
| best-EA vs thesis structural distance | 0.33 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +2.8 |

Thesis route-set on TEST seeds: score 1250.80, wins 8/8, served ~26, lost ~0.0.

Ladder (median TEST score vs budget): 100:1261  400:1261  1600:1261  6400:1261 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  9 cells, serves ["A", "B", "C"]
card 1 line  9 cells, serves ["A", "B", "C"]
```

**Verdict.** SOFT-BORDER: a broad basin — 100.00% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap +0.3 over 0 ladder step(s). The EA beats the hand thesis (+10.1). Exhaustive K = 6.

## R-4 — Big Store   [soft-border]

Thesis: *a department store, offices and a cafe - lots of empty grid, route it your way*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 91.33%, 548/600 legal draws; legality 2.1%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 8/8 seeds.

5 rooms, 3 cards, quota 18 / max_lost 10. Search cost: 13536 runs, 349 s wall.

**Class driven by:** legal-plan win rate 91.33% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 2.1% (600 legal of 28726) |
| **legal-plan win rate** (freer sampler) | **91.33%** (548/600 legal draws win) |
| solvability (per-seed: solver win exists) | 8/8 -> solvable |
| best single EA genome generalises (wins on TEST) | 8/8 |
| `K` archive estimate | 729 |
| `K` EXHAUSTIVE (ground truth) | n/a — space too large (392^3 = 60236288 genomes > cap 60000); archive estimate used instead |
| `e1` (evals to first win) | 8 |
| `skill_gap` (ea@B - random best) | +2.9 |
| ladder steps (budget doublings that beat noise 0.57) | 3 |
| best-EA vs thesis structural distance | 0.50 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +2.9 |

Thesis route-set on TEST seeds: score 1240.30, wins 8/8, served ~18, lost ~0.0.

Ladder (median TEST score vs budget): 100:1232  400:1236*  1600:1239*  6400:1242* -> **3 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line 10 cells, serves ["B", "C", "E"]
card 1 line  3 cells, serves ["A", "B"]
card 2 line  7 cells, serves ["B", "D"]
```

**Verdict.** SOFT-BORDER: a broad basin — 91.33% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap +2.9 over 3 ladder step(s). The EA beats the hand thesis (+1.5). Exhaustive K n/a (space too large; archive K = 729).

## R-5 — Bottleneck   [soft-border]

Thesis: *one width-2 corridor, two lifts - only one fits, so they take turns*

**Classifier:** legal random plans mostly WIN (legal-plan win rate 87.17%, 523/600 legal draws; legality 8.4%) — a forgiving optimization landscape with a broad basin of working plans; the solver also wins 8/8 seeds.

4 rooms, 2 cards, quota 12 / max_lost 8. Search cost: 11148 runs, 136 s wall.

**Class driven by:** legal-plan win rate 87.17% >= 40% (broad basin)

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 8.4% (600 legal of 7153) |
| **legal-plan win rate** (freer sampler) | **87.17%** (523/600 legal draws win) |
| solvability (per-seed: solver win exists) | 8/8 -> solvable |
| best single EA genome generalises (wins on TEST) | 8/8 |
| `K` archive estimate | 60 |
| `K` EXHAUSTIVE (ground truth) | 60  — 1296 genomes enumerated (stops<=3), 66 distinct served-room structures, 60 winning (>=4/8 seeds) |
| `e1` (evals to first win) | 8 |
| `skill_gap` (ea@B - random best) | -3.3 |
| ladder steps (budget doublings that beat noise 1.13) | 0 |
| best-EA vs thesis structural distance | 0.75 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +1.4 |

Thesis route-set on TEST seeds: score 1252.40, wins 8/8, served ~13, lost ~0.0.

Ladder (median TEST score vs budget): 100:1251  400:1251  1600:1251  6400:1251 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  9 cells, serves ["B", "C", "D"]
card 1 line  8 cells, serves ["A", "B", "C"]
```

**Verdict.** SOFT-BORDER: a broad basin — 87.17% of legal random plans win — so the level has many working plans and quality varies smoothly; skill_gap -3.3 over 0 ladder step(s). The EA trails the hand thesis (-1.7). Exhaustive K = 60.

## R-6 — Squeeze   [puzzle]

Thesis: *a capped shared shaft - two lifts must SPLIT it and hand off at the transfer*

**Classifier:** legal random plans mostly LOSE (legal-plan win rate 2.17%, 13/600 legal draws; legality 2.9% of raw draws are even legal under the caps) but the solver reliably WINS (solvable on 8/8 seeds, first win at 56 evals) — a constraint puzzle whose win requires a specific cooperative structure. (For reference, the spec's raw w_rand rule would say 'soft-border'.)

5 rooms, 2 cards, quota 24 / max_lost 8. Search cost: 9749 runs, 168 s wall.

**Class driven by:** legal-plan win rate 2.17% < 40% (few work) AND solvable 8/8

| metric | value |
|---|---|
| legality rate (legal raw draws / attempts) | 2.9% (600 legal of 20376) |
| **legal-plan win rate** (freer sampler) | **2.17%** (13/600 legal draws win) |
| solvability (per-seed: solver win exists) | 8/8 -> solvable |
| best single EA genome generalises (wins on TEST) | 5/8 |
| `K` archive estimate | 10 |
| `K` EXHAUSTIVE (ground truth) | 1  — 6400 genomes enumerated (stops<=3), 34 distinct served-room structures, 1 winning (>=4/8 seeds) |
| `e1` (evals to first win) | 56 |
| `skill_gap` (ea@B - random best) | +0.0 |
| ladder steps (budget doublings that beat noise 1.45) | 0 |
| best-EA vs thesis structural distance | 0.00 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +5.3 |

Thesis route-set on TEST seeds: score 1242.20, wins 6/8, served ~24, lost ~4.3.

Ladder (median TEST score vs budget): 100:1242  400:1240  1600:1240  6400:1240 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  6 cells, serves ["A", "B", "E"]
card 1 line  6 cells, serves ["C", "D", "E"]
```

**Verdict.** PUZZLE: only 2.17% of legal random plans win (the overlap cap forces a specific cooperative structure — the freer sampler almost never stumbles into it), but the solver finds a legal win on 8/8 seeds (first win at 56 evals). The best single genome generalises to 5/8 test seeds (a fragile win). Exhaustive K = 1. The intended cooperative split IS among the winning niches.

## Method caveats / what I don't fully trust

- **Solver-relative solvability.** "Solvable" is per-seed: for >=7/8 TEST seeds, at least one route-set the search discovered as a winner (a niche example, the best genome, or the hand thesis) wins that seed within the budget. It is NOT "one genome wins every seed" — R-6's cooperative split is a legal win but seed-fragile (the best single genome generalises to only 5/8 test seeds), so requiring one genome to win 7/8 would falsely read R-6 as unsolvable. A false BROKEN (our decoder missed a real win) and a false GOOD PUZZLE (the solver found a path no human would) are the two states this most easily confuses (docs/v5-transition-spec.md §6). The numbers are a filter, never the verdict.
- **The thesis is used as a known-legal winner in the solvability check.** That makes solvability not strictly solver-only for the shipped levels; the per-seed count would be re-derived from solver-found winners alone before this gates NEW (un-thesised) levels.
- **K is a secondary quality signal, not the classifier's discriminator.** The archive `K` (distinct `win_signature`s the EA evaluated) does NOT separate puzzle from soft on this tiny suite — signature granularity (overlap-cell counts, loop flags) can give a puzzle a HIGHER archive K than a tutorial. Where the capped stop-sequence space is small enough, **exhaustive K** enumerates all winning per-card served-room structures (ground truth, Sturtevant EPCG); where the space is too large (R-4, with 8 docks x 3 cards) it falls back to the archive estimate, stated as such. Exhaustive K enumerates dock-stop sequences up to length 3 and card-decode-order permutations, so a winner reachable only by a longer sequence would be missed — a known blind spot on the enumerated levels.
- **The freer sampler's legality rate conflates two things**: how constraining the caps are AND how often a drifted self-avoiding walk dead-ends or misses a room on a given geometry. So a low legality rate (e.g. R-4 at ~2%%, which has NO caps) is not by itself evidence of a constraint puzzle — the classifier reads the legal-plan WIN rate, not legality, for exactly this reason.
- **e1 is a within-level relative difficulty**, evals-to-first-win for THIS solver — not human difficulty (Sturtevant 2020).
- The decoder is order-dependent under the overlap cap (card 0 claims capacity first); a reorder-cards mutation lets the search rearrange it, but a solution only reachable by a decode order the mutation never tried could read as harder than it is.
