# v5 depth report — GENERATED by tools/v5/run_depth5.gd

Do not hand-edit: re-run the depth tool instead. Generated 2026-08-14T09:53:32.

## How to read this

- ONE search pass per level yields TWO readings (docs/v5-transition-spec.md §2.2): Reading A (soft-border) = random-win-rate + `skill_gap` + the ladder; Reading B (puzzle) = solvability + `K` + `e1`. The **auto-classifier** picks the headline.
- **spec-primary** is the spec §2.3 rule applied verbatim: `w_rand >= 5` (>= 5 decodable random wins out of >=600 samples, ~0.8%) -> soft-border; `w_rand < 5` with a solver win on >=7/8 seeds -> puzzle; else BROKEN.
- **class** is the corrected call. The spec-primary rule assumes a puzzle has random-win ~0 (`w_rand < 5`); on this tiny suite that assumption fails for R-6 (see its diagnosis), and `K` cannot rescue it (signature granularity gives R-6 a HIGHER K than the tutorials). The robust divider is the **win/loss balance of LEGAL random plans** against the spec's own forgiving bar (§4.1, rwr >= 40%): random mostly WINS -> **soft-border** (forgiving landscape); random mostly LOSES but the solver reliably wins -> **puzzle** (constraint); random ~never wins and no solver win -> **BROKEN**.
- **Solvability is per-seed** (spec §2.2a): for >= 7/8 TEST seeds, does the solver's set of discovered winning route-sets contain one that wins that seed? (Not "does one genome win every seed" — the right question when a legal win is seed-fragile.)
- One budget unit = one sim run of THE REAL v5 LEVEL (shipped quota / max_lost / win-lose live, 300 game-second timeout). `WIN -> 1000 + (300 - t_win)`; else `served - 3.0*lost - 0.01*avg_wait`. Optimizers search TRAIN seeds at STEP 0.25; every reported number is a median over the disjoint TEST seeds at STEP 0.10.
- Solvability is always **relative to this solver at this budget** (Ropossum's caveat): "winnable by our EA within 6400 evals on these seeds", never "winnable".

## Classification table

`class` is the corrected call; `spec-primary` is the spec §2.3 w_rand rule applied verbatim. They differ only where the w_rand rule miscalls (see the R-6 diagnosis).

| level | class | spec-primary | w_rand (rwr) | solvable seeds | K | e1 evals | skill_gap | ladder steps | thesis among niches |
|---|---|---|---|---|---|---|---|---|---|
| **R-1** One Lift | **soft-border** | soft-border | 477 (79.50%) | 8/8 | 4 | 8 | +0.0 | 0 | yes |
| **R-2** Double Duty | **soft-border** | soft-border | 600 (100.00%) | 8/8 | 1 | 8 | +0.0 | 0 | yes |
| **R-3** Crossing | **soft-border** | soft-border | 600 (100.00%) | 8/8 | 21 | 8 | +0.3 | 0 | yes |
| **R-4** Big Store | **soft-border** | soft-border | 517 (86.17%) | 8/8 | 729 | 8 | +2.9 | 3 | yes |
| **R-5** Bottleneck | **soft-border** | soft-border | 565 (94.17%) | 8/8 | 60 | 8 | -3.3 | 0 | yes |
| **R-6** Squeeze | **puzzle** | soft-border ⚠ | 38 (6.33%) | 8/8 | 10 | 56 | +0.0 | 0 | yes |

## Validation of the classifier against hand-understood levels

- **R-1 "One Lift" expected SOFT-BORDER** — got **soft-border**. CORRECT: a forgiving tutorial with a real random-win floor.
- **R-6 "Squeeze" expected PUZZLE** — got **puzzle** (corrected). CORRECT. Legal random plans mostly LOSE (rwr 6.33%) and the solver is solvable 8/8 seeds via the cooperative split, and the intended split IS among the winning niches. NOTE: the spec-primary w_rand rule alone says **soft-border** (a MISCALL) because R-6's random-win is ~6%, NOT ~0 — the dock/shortest-path decoder auto-covers intermediate rooms and auto-splits at the transfer, and illegal overlaps are filtered as undecodable rather than counted as losses. This is the signal that the w_rand threshold (and K) needs the rwr-balance refinement before gating new puzzles.

## R-1 — One Lift   [soft-border]

Thesis: *one lift, three rooms - draw a line past them all*

**Classifier:** legal random plans mostly WIN (rwr=79.50%, w_rand=477>=5) — a forgiving optimization landscape with a real floor; the solver also wins 8/8.

3 rooms, 1 cards, quota 10 / max_lost 8. Search cost: 872 runs, 5 s wall.

| metric | value |
|---|---|
| random-win count `w_rand` | 477 of 600 decodable (rwr 79.50%) |
| solvability (per-seed: solver win exists) | 8/8 -> solvable |
| best single EA genome generalises (wins on TEST) | 8/8 |
| `K` (structurally-distinct winning route-sets) | 4 |
| `e1` (evals to first win) | 8 |
| `skill_gap` (ea@B - random best) | +0.0 |
| ladder steps (budget doublings that beat noise 1.10) | 0 |
| best-EA vs thesis structural distance | 0.00 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +2.4 |

Thesis route-set on TEST seeds: score 1255.20, wins 8/8, served ~11, lost ~0.0.

Ladder (median TEST score vs budget): 100:1255  400:1255  1600:1255  6400:1255 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  7 cells, serves ["A", "B", "C"]
```

**Verdict.** SOFT-BORDER: 477 of 600 decodable random plans win (rwr 79.50%), so the level has a measurable floor and quality varies smoothly; skill_gap +0.0 over 0 ladder step(s). The EA matches the hand thesis (+0.0).

## R-2 — Double Duty   [soft-border]

Thesis: *one column can serve two rooms at once - find the cell that touches both*

**Classifier:** legal random plans mostly WIN (rwr=100.00%, w_rand=600>=5) — a forgiving optimization landscape with a real floor; the solver also wins 8/8.

3 rooms, 1 cards, quota 12 / max_lost 8. Search cost: 722 runs, 3 s wall.

| metric | value |
|---|---|
| random-win count `w_rand` | 600 of 600 decodable (rwr 100.00%) |
| solvability (per-seed: solver win exists) | 8/8 -> solvable |
| best single EA genome generalises (wins on TEST) | 8/8 |
| `K` (structurally-distinct winning route-sets) | 1 |
| `e1` (evals to first win) | 8 |
| `skill_gap` (ea@B - random best) | +0.0 |
| ladder steps (budget doublings that beat noise 0.35) | 0 |
| best-EA vs thesis structural distance | 0.00 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +0.8 |

Thesis route-set on TEST seeds: score 1257.10, wins 8/8, served ~13, lost ~0.0.

Ladder (median TEST score vs budget): 100:1257  400:1257  1600:1257  6400:1257 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  5 cells, serves ["A", "B", "C"]
```

**Verdict.** SOFT-BORDER: 600 of 600 decodable random plans win (rwr 100.00%), so the level has a measurable floor and quality varies smoothly; skill_gap +0.0 over 0 ladder step(s). The EA matches the hand thesis (+0.0).

## R-3 — Crossing   [soft-border]

Thesis: *two lifts share the atrium - ride in, cross over, ride on*

**Classifier:** legal random plans mostly WIN (rwr=100.00%, w_rand=600>=5) — a forgiving optimization landscape with a real floor; the solver also wins 8/8.

3 rooms, 2 cards, quota 24 / max_lost 400. Search cost: 13516 runs, 419 s wall.

| metric | value |
|---|---|
| random-win count `w_rand` | 600 of 600 decodable (rwr 100.00%) |
| solvability (per-seed: solver win exists) | 8/8 -> solvable |
| best single EA genome generalises (wins on TEST) | 8/8 |
| `K` (structurally-distinct winning route-sets) | 21 |
| `e1` (evals to first win) | 8 |
| `skill_gap` (ea@B - random best) | +0.3 |
| ladder steps (budget doublings that beat noise 0.42) | 0 |
| best-EA vs thesis structural distance | 0.33 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +2.8 |

Thesis route-set on TEST seeds: score 1250.80, wins 8/8, served ~26, lost ~0.0.

Ladder (median TEST score vs budget): 100:1261  400:1261  1600:1261  6400:1261 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  9 cells, serves ["A", "B", "C"]
card 1 line  9 cells, serves ["A", "B", "C"]
```

**Verdict.** SOFT-BORDER: 600 of 600 decodable random plans win (rwr 100.00%), so the level has a measurable floor and quality varies smoothly; skill_gap +0.3 over 0 ladder step(s). The EA beats the hand thesis (+10.1).

## R-4 — Big Store   [soft-border]

Thesis: *a department store, offices and a cafe - lots of empty grid, route it your way*

**Classifier:** legal random plans mostly WIN (rwr=86.17%, w_rand=517>=5) — a forgiving optimization landscape with a real floor; the solver also wins 8/8.

5 rooms, 3 cards, quota 18 / max_lost 10. Search cost: 13536 runs, 381 s wall.

| metric | value |
|---|---|
| random-win count `w_rand` | 517 of 600 decodable (rwr 86.17%) |
| solvability (per-seed: solver win exists) | 8/8 -> solvable |
| best single EA genome generalises (wins on TEST) | 8/8 |
| `K` (structurally-distinct winning route-sets) | 729 |
| `e1` (evals to first win) | 8 |
| `skill_gap` (ea@B - random best) | +2.9 |
| ladder steps (budget doublings that beat noise 0.57) | 3 |
| best-EA vs thesis structural distance | 0.50 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +2.9 |

Thesis route-set on TEST seeds: score 1240.30, wins 8/8, served ~18, lost ~0.0.

Ladder (median TEST score vs budget): 100:1232  400:1236*  1600:1239*  6400:1242* -> **3 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line 10 cells, serves ["B", "C", "E"]
card 1 line  3 cells, serves ["A", "B"]
card 2 line  7 cells, serves ["B", "D"]
```

**Verdict.** SOFT-BORDER: 517 of 600 decodable random plans win (rwr 86.17%), so the level has a measurable floor and quality varies smoothly; skill_gap +2.9 over 3 ladder step(s). The EA beats the hand thesis (+1.5).

## R-5 — Bottleneck   [soft-border]

Thesis: *one width-2 corridor, two lifts - only one fits, so they take turns*

**Classifier:** legal random plans mostly WIN (rwr=94.17%, w_rand=565>=5) — a forgiving optimization landscape with a real floor; the solver also wins 8/8.

4 rooms, 2 cards, quota 12 / max_lost 8. Search cost: 10620 runs, 124 s wall.

| metric | value |
|---|---|
| random-win count `w_rand` | 565 of 600 decodable (rwr 94.17%) |
| solvability (per-seed: solver win exists) | 8/8 -> solvable |
| best single EA genome generalises (wins on TEST) | 8/8 |
| `K` (structurally-distinct winning route-sets) | 60 |
| `e1` (evals to first win) | 8 |
| `skill_gap` (ea@B - random best) | -3.3 |
| ladder steps (budget doublings that beat noise 1.13) | 0 |
| best-EA vs thesis structural distance | 0.75 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +1.4 |

Thesis route-set on TEST seeds: score 1252.40, wins 8/8, served ~13, lost ~0.0.

Ladder (median TEST score vs budget): 100:1251  400:1251  1600:1251  6400:1251 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  9 cells, serves ["B", "C", "D"]
card 1 line  8 cells, serves ["A", "B", "C"]
```

**Verdict.** SOFT-BORDER: 565 of 600 decodable random plans win (rwr 94.17%), so the level has a measurable floor and quality varies smoothly; skill_gap -3.3 over 0 ladder step(s). The EA trails the hand thesis (-1.7).

## R-6 — Squeeze   [puzzle]

Thesis: *a capped shared shaft - two lifts must SPLIT it and hand off at the transfer*

**Classifier:** legal random plans mostly LOSE (rwr=6.33%, 38/600 wins) but the solver reliably WINS (solvable on 8/8 seeds, first win at 56 evals) — a constraint puzzle whose win requires structure. Spec's w_rand rule would say 'soft-border' (w_rand=38>=5), a MISCALL here: the dock/shortest-path decoder inflates R-6's random-win above ~0, so w_rand cannot see the puzzle; the rwr win/loss balance (spec §4.1 forgiving bar 40%) does.

5 rooms, 2 cards, quota 24 / max_lost 8. Search cost: 9477 runs, 140 s wall.

| metric | value |
|---|---|
| random-win count `w_rand` | 38 of 600 decodable (rwr 6.33%) |
| solvability (per-seed: solver win exists) | 8/8 -> solvable |
| best single EA genome generalises (wins on TEST) | 5/8 |
| `K` (structurally-distinct winning route-sets) | 10 |
| `e1` (evals to first win) | 56 |
| `skill_gap` (ea@B - random best) | +0.0 |
| ladder steps (budget doublings that beat noise 1.45) | 0 |
| best-EA vs thesis structural distance | 0.00 |
| intended solution among winning niches | yes |
| seed fragility (train - test) | +5.3 |

Thesis route-set on TEST seeds: score 1242.20, wins 6/8, served ~24, lost ~4.3.

Ladder (median TEST score vs budget): 100:1242  400:1240  1600:1240  6400:1240 -> **0 step(s)**.

Best route-set found (served rooms per card):

```
card 0 line  6 cells, serves ["A", "B", "E"]
card 1 line  6 cells, serves ["C", "D", "E"]
```

**Verdict.** PUZZLE: legal random plans mostly LOSE (rwr 6.33%, 38/600) because the overlap cap forces a specific cooperative structure, but the solver finds a legal win on 8/8 seeds (first win at 56 evals). K=10 archive winning-signature(s); the best single genome generalises to 5/8 test seeds (a fragile win). The intended cooperative split IS among the winning niches.

## Method caveats / what I don't fully trust

- **Solver-relative solvability.** "Solvable" is per-seed: for >=7/8 TEST seeds, at least one route-set the search discovered as a winner (a niche example, the best genome, or the hand thesis) wins that seed within the budget. It is NOT "one genome wins every seed" — R-6's cooperative split is a legal win but seed-fragile (the best single genome generalises to only 5/8 test seeds), so requiring one genome to win 7/8 would falsely read R-6 as unsolvable. A false BROKEN (our decoder missed a real win) and a false GOOD PUZZLE (the solver found a path no human would) are the two states this most easily confuses (docs/v5-transition-spec.md §6). The numbers are a filter, never the verdict.
- **The thesis is used as a known-legal winner in the solvability check.** That makes solvability not strictly solver-only for the shipped levels; the per-seed count would be re-derived from solver-found winners alone before this gates NEW (un-thesised) levels.
- **K is archive-derived, not exhaustive.** K counts distinct `win_signature`s (per-card served-room set + loop/overlap flags) among winning genomes the EA happened to evaluate. It is a lower bound on true solution diversity and only as honest as the search's coverage; a proper ground-truth K needs exhaustive stop-sequence enumeration under the cap (Sturtevant EPCG), not yet built here.
- **e1 is a within-level relative difficulty**, evals-to-first-win for THIS solver — not human difficulty (Sturtevant 2020).
- The decoder is order-dependent under the overlap cap (card 0 claims capacity first); a reorder-cards mutation lets the search rearrange it, but a solution only reachable by a decode order the mutation never tried could read as harder than it is.
